<application>
  <component name="GraziConfig">
    <option name="enabledLanguages">
      <set>
        <option value="English (US)" />
        <option value="Persian" />
      </set>
    </option>
    <option name="suppressingContext">
      <SuppressingContext>
        <option name="suppressed">
          <set>
            <option value="Process the refund state machine and update the manage data status according to related state First, find related handler, Next, call find_next_status on that handler, If there is a next status, find the next task name and send the task on celery.|Process the refund state machine and update the manage data status according to related state First, find related handler, Next, call find_next_status on that handler, If there is a next status, find the next task name and send the task on celery." />
            <option value="Ha|Handles the case when changing the status of a payment is not allowed." />
            <option value="process the refund state machine and update the manage data status according to related state first, find related handler, next, call find_next_status on that handler, if there is a next status, find the next task name and send the task on celery.|process the refund state machine and update the manage data status according to related state first, find related handler, next, call find_next_status on that handler, if there is a next status, find the next task name and send the task on celery." />
            <option value="are|finotech use the other provider, this code meens the fintech providers are not available" />
            <option value="fucking" />
            <option value="This method logs the ServiceInvocation, gets the banned sender for the specified service, makes the call to send_request method of service, Starts the task to log the report provided by the service after making the call, starts the task that checks whether the sender should be banned temporarily for the user using this service and starts the task that checks whether the RequestSupervisor needs to refresh its config from database RequestSupervisor The service to call its send_request.|This method logs the ServiceInvocation, gets the banned sender for the specified service, makes the call to send_request method of service, Starts the task to log the report provided by the service after making the call, starts the task that checks whether the sender should be banned temporarily for the user using this service and starts the task that checks whether the RequestSupervisor needs to refresh its config from database RequestSupervisor The service to call its send_request." />
            <option value="The user causing the service invocation The Model this service was invoked for The id of the user causing the service invocation The id of the model this service was invoked for The id of the type of the model this service was invoked for The data to pass to send_request method of Service The sender to use for this request regardless of anything else The sender to use for this request before anything else Boolean that denotes whether this is a bulk invocation The function to call if the invocation reaches a delivered status in recovery None|The user causing the service invocation The Model this service was invoked for The id of the user causing the service invocation The id of the model this service was invoked for The id of the type of the model this service was invoked for The data to pass to send_request method of Service The sender to use for this request regardless of anything else The sender to use for this request before anything else Boolean that denotes whether this is a bulk invocation The function to call if the invocation reaches a delivered status in recovery None" />
          </set>
        </option>
      </SuppressingContext>
    </option>
  </component>
</application>