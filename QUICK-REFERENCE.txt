┌──────────────────────────────────────────────────────────────────┐
│                    DOTFILES V3 - QUICK REFERENCE                 │
└──────────────────────────────────────────────────────────────────┘

📍 FIRST TIME SETUP (Current Machine)
┌──────────────────────────────────────────────────────────────────┐
│ 1. ./scripts/first-setup.sh              ← Interactive guide     │
│                                          (RECOMMENDED)            │
│ OR manually:                                                     │
│ 1. make backup                           ← Backup everything     │
│ 2. git status                            ← Review changes        │
│ 3. make check                            ← Check sensitive files │
│ 4. make commit                           ← Commit changes        │
│ 5. make setup-git                        ← Add remote repo       │
│ 6. make push                             ← Push to GitHub        │
└──────────────────────────────────────────────────────────────────┘

🖥️  NEW MACHINE SETUP (Restore)
┌──────────────────────────────────────────────────────────────────┐
│ 1. git clone <url> ~/dotfiles                                    │
│ 2. cd ~/dotfiles                                                 │
│ 3. ./install.sh                          ← One command install   │
│ 4. Restart/logout                        ← Apply changes         │
└──────────────────────────────────────────────────────────────────┘

⚡ COMMON COMMANDS
┌──────────────────────────────────────────────────────────────────┐
│ make help                                ← Show all commands     │
│ make backup                              ← Full system backup    │
│ make install                             ← Full installation     │
│ make sync                                ← Pull→Backup→Push      │
│ make link                                ← Link configs          │
│ make status                              ← Git status            │
└──────────────────────────────────────────────────────────────────┘

🎯 SELECTIVE OPERATIONS
┌──────────────────────────────────────────────────────────────────┐
│ Backup:                                                          │
│   make backup-packages                   ← Only packages         │
│   make backup-gnome                      ← Only GNOME            │
│   make backup-configs                    ← Only configs          │
│                                                                  │
│ Install:                                                         │
│   ./install.sh --skip-packages           ← Skip packages         │
│   ./install.sh --skip-gnome              ← Skip GNOME            │
│   ./install.sh --skip-configs            ← Skip configs          │
└──────────────────────────────────────────────────────────────────┘

📂 DIRECTORY STRUCTURE
┌──────────────────────────────────────────────────────────────────┐
│ configs/          → App configs (managed by GNU Stow)            │
│ packages/         → Package lists (apt, flatpak, snap, pip, npm) │
│ gnome/            → GNOME extensions & settings (dconf dumps)    │
│ scripts/          → Automation scripts                           │
│ fonts/            → Custom fonts                                 │
└──────────────────────────────────────────────────────────────────┘

📚 DOCUMENTATION
┌──────────────────────────────────────────────────────────────────┐
│ GETTING-STARTED.md           ← START HERE!                       │
│ QUICKSTART.md                ← Quick walkthrough                 │
│ MIGRATION-CHECKLIST.md       ← Complete migration guide          │
│ CONTRIBUTING.md              ← Customization & advanced          │
│ README.md                    ← Main documentation                │
└──────────────────────────────────────────────────────────────────┘

🛠️  TROUBLESHOOTING
┌──────────────────────────────────────────────────────────────────┐
│ Scripts won't run:                                               │
│   chmod +x install.sh scripts/*.sh                               │
│                                                                  │
│ Stow conflicts:                                                  │
│   mv ~/.bashrc ~/.bashrc.backup          ← Backup existing       │
│   cd configs && stow --adopt -t ~ bash   ← Or adopt files        │
│                                                                  │
│ Package install fails:                                           │
│   → Normal on different distros, script continues                │
│                                                                  │
│ GNOME extensions not working:                                    │
│   → Must install manually from extensions.gnome.org              │
│   → Settings will auto-restore after install                     │
└──────────────────────────────────────────────────────────────────┘

🔧 ADDING NEW CONFIGS
┌──────────────────────────────────────────────────────────────────┐
│ 1. mkdir -p configs/myapp/.config/myapp                          │
│ 2. cp ~/.config/myapp/* configs/myapp/.config/myapp/             │
│ 3. cd configs && stow -t ~ myapp                                 │
│ 4. git add configs/myapp                                         │
│ 5. make commit && make push                                      │
└──────────────────────────────────────────────────────────────────┘

🔄 KEEPING DOTFILES SYNCED
┌──────────────────────────────────────────────────────────────────┐
│ Daily workflow:                                                  │
│   make sync                              ← Pull, backup, push    │
│                                                                  │
│ On any machine:                                                  │
│   cd ~/dotfiles && make pull             ← Get latest            │
│   ./install.sh                           ← Apply updates         │
└──────────────────────────────────────────────────────────────────┘

🎯 WHAT GETS BACKED UP
┌──────────────────────────────────────────────────────────────────┐
│ ✓ All system packages (apt/dnf/pacman, flatpak, snap, pip, npm) │
│ ✓ GNOME extensions + settings                                   │
│ ✓ Complete dconf configuration                                  │
│ ✓ Keybindings (window manager + media)                          │
│ ✓ Application configs (shell, git, vim, vscode, etc.)           │
│ ✓ VS Code extensions                                            │
│ ✓ GTK themes and preferences                                    │
│ ✗ SSH/GPG keys (handled separately for security)                │
└──────────────────────────────────────────────────────────────────┘

💡 PRO TIPS
┌──────────────────────────────────────────────────────────────────┐
│ • Test in a VM before relying on it                              │
│ • Run 'make check' before committing to catch sensitive files    │
│ • Keep a separate private repo for secrets (SSH keys, etc.)      │
│ • Set up a weekly cron job: make sync                            │
│ • Document any manual steps in your own notes                    │
│ • Use branches for different machines if needed                  │
└──────────────────────────────────────────────────────────────────┘

🆘 NEED HELP?
┌──────────────────────────────────────────────────────────────────┐
│ ./scripts/first-setup.sh     ← Interactive helper               │
│ make help                    ← Available commands               │
│ Read the documentation       ← Comprehensive guides             │
│ bash -x script.sh            ← Debug mode                       │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                    READY TO START? RUN:                          │
│                 ./scripts/first-setup.sh                         │
└──────────────────────────────────────────────────────────────────┘

